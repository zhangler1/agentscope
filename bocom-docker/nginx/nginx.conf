# nginx 网关：统一入口，代理前端与后端 API
# 以 envsubst 模板挂载到 /etc/nginx/templates/default.conf.template，
# ${FRONTEND_UPSTREAM} 由 compose 环境变量注入（webui-frontend:80）。

# HMR / WebSocket 升级支持（浏览器带 Upgrade 头时透传，否则保持 close）
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

server {
    listen 80;
    server_name _;

    # 上传/大文件限制
    client_max_body_size 100m;

    # gzip 压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript image/svg+xml;

    # API 请求转发到 Agent Service（剥掉 /api 前缀：/api/chat → /chat）。
    # webui 前端统一以 /api 前缀访问（client.ts：API_PREFIX 默认 /api），
    # 而 agentscope 后端路由本身不带 /api（如 /deerflow/threads、/files），
    # 因此网关剥掉前缀后再转发。
    location /api/ {
        proxy_pass http://agentscope:8000/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # SSE 流式响应：关闭缓冲，拉长超时
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
        chunked_transfer_encoding on;
    }

    # 其余请求转发到前端（SPA 回退/资源均由其处理）
    location / {
        proxy_pass http://${FRONTEND_UPSTREAM};
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # vite HMR 的 WebSocket 透传
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
    }
}
