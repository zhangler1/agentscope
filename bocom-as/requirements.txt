# bocom-as requirements（锁定版本）
# 由 pip 在干净 Python 3.12 环境中解析 bocom-as 0.1.0 的完整依赖闭包生成。
# 覆盖：pyproject.toml 主依赖列表（agentscope==2.0.7 传递依赖 + service /
# storage-redis / workspace-docker 三个部署必需 extras）+ providers 直接依赖。
# 用法：
#   pip install -r requirements.txt          # 安装依赖（不含 bocom-as 本身）
#   pip install -e .                         # 再以可编辑模式安装 bocom-as
# 注意：本文件仅锁依赖；bocom-as 本体请单独安装（自包含发行版）。

# ---- 直接依赖（与 pyproject.toml 一致）----
agentscope==2.0.5

fastapi>=0.141.1
uvicorn>=0.52.4
apscheduler>=3.11.3
ag-ui-protocol>=0.1.10
redis>=8.1.0 
aiodocker>=0.27.0

# ---- providers 直接依赖 ----
httpx>=0.28.1
pydantic>=2.13.5
